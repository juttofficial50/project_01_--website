* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
  font-family: Poppins, sans-serif;
}

header {
  background: #f85606; /* Daraz orange */
  padding: 10px 0;
}

.container {
  width: 95%;
  margin: auto;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 15px;
}

.items h1 {
  color: white;
  font-size: 28px;
  font-weight: 700;
  display: flex;
  align-items: center;
  gap: 5px;
}

/* Search Bar */
form {
  display: flex;
  align-items: center;
  background: white;
  border-radius: 5px;
  overflow: hidden;
  border: 2px solid transparent;
}

form input {
  padding: 10px;
  width: 250px;
  border: none;
  outline: none;
}

form button {
  padding: 10px 15px;
  background: #333;
  color: white;
  border: none;
  cursor: pointer;
}

form button:hover {
  background: black;
}

/* Icons & Links */
.items {
  color: white;
  font-size: 14px;
}

.items a {
  text-decoration: none;
  color: white;
  font-weight: 500;
}

.items a:hover {
  text-decoration: underline;
}

/* Cart icon */
.items:last-child,
.items:nth-last-child(3) {
  font-size: 18px;
}